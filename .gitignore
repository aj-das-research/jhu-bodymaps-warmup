# Bulk inputs fetched by scripts/download_data.sh — individual files exceed
# GitHub's 100 MB hard limit (ct 327 MB, tarball 439 MB, checkpoint 754 MB)
data/
*.pth

# Upstream clones (nested git repos; recreated by the setup scripts)
third_party/SuPreM/
third_party/ShapeKit/

# Sandbox trash pending manual delete
_to_delete/

__pycache__/
.ipynb_checkpoints/
.DS_Store

# Report source (kept out of git; rendered on Overleaf)
paper/
paper_folder.tar.gz
hpc guide/
